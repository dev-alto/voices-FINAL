# Environment Variables

This project requires the following environment variables to be set in your `.env.local` file:

## Required API Keys

### Gemini API (Google AI)
```
GEMINI_API_KEY=your_gemini_api_key_here
```
- Used for: Text analysis, interview responses, document processing
- Get your key from: https://makersuite.google.com/app/apikey

### Fish Audio TTS API
```
TTS_FISH_AUDIO_API_KEY=your_fish_audio_api_key_here
TTS_ENDPOINT=https://api.fish.audio/v1/tts
TTS_ENABLED=true
```
- Used for: Text-to-speech conversion for agent voices
- Get your key from: https://fish.audio/

## Example .env.local file

Create a `.env.local` file in the root directory with:

```env
GEMINI_API_KEY=your_gemini_api_key_here
TTS_FISH_AUDIO_API_KEY=your_fish_audio_api_key_here
TTS_ENDPOINT=https://api.fish.audio/v1/tts
TTS_ENABLED=true
```

**Note:** Never commit `.env.local` to version control. It's already in `.gitignore`.
